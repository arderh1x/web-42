/* step 4 */
console.log("Script A (async) executed.");

// > зафіксувати фактичний момент появи повідомлення:
// async: executes when loaded (if second in order with sync as first - cannot be first, but isn't guaranteed to always be second)